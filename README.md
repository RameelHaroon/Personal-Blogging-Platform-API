# Personal-Blogging-Platform-API

A simple RESTful API built with Spring Boot that allows users to create, read, update, and delete blog posts. The project demonstrates key Spring Boot concepts including CRUD operations, REST controllers, JPA with a relational database running inside a docker container, and exception handling.
